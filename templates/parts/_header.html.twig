<header>

	<nav class="navbar navbar-expand navbar-dark bg-success container-fluid justify-content-end">
		{% if app.user %}

			<a class="nav-link dropdown-toggle text-white" data-bs-toggle="dropdown" aria="false">{{ app.user.nom }}</a>
			<ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
				<li>
					<a class="dropdown-item dropdown-logout" href="{{ path('app_logout') }}">Se déconnecter</a>
				</li>
			{% else %}
				<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">
							<i class="far fa-user"></i>
							Se connecter</a>
					</li>
				</ul>

			{% endif %}

		</div>
	</nav>
	<nav class="navbar navbar-expand-lg navbar-light bg-white">
		<div class="container-fluid">
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse justify-content-around" id="navbarSupportedContent">
				<ul class="navbar-nav mb-2 mb-lg-0">
					{% if app.user %}
						<li class="nav-item1">
							<a class="nav-link active text-dark" aria-current="page" href="{{path('home')}}">Accueil</a>
							
						</li>
						<li class="nav-item1">
							<a class="nav-link active text-dark" aria-current="page" href="{{path('moncompte')}}">Mon compte</a>
						</li>
						<li class="nav-item2">
							<a class="nav-link text-dark" href="{{path('panier')}}">
								<i class="fas fa-shopping-cart"></i>
								Panier</a>
						</li>
					</li><li class="nav-item3">
						<a class="nav-link text-dark" href="{{path('aproposdenous')}}">À propos de nous</a>
					</li>
					<li class="nav-item4">
						<a class="nav-link text-dark" href="{{path('contact')}}">Contactez nous</a>
					</li>
				</ul>
				{#
				<form class="d-flex">
				<input class="form-control" type="search" placeholder="Rechercher..." aria-label="Search">
					<button class="btn btn-outline-success" type="submit">
						<i class="fa fa-search"></i>
					</button>
				</form>#}

			{% else %}
				<li class="nav-item1">
					<a class="nav-link active text-dark" aria-current="page" href="{{path('home')}}">Accueil</a>
				</li>
				<li class="nav-item2">
					<a class="nav-link active text-dark" aria-current="page" href="{{ path('app_register')}}">
						<i class="far fa-user"></i>Créer un compte</a>
				</li>
				<li class="nav-item3">
					<a class="nav-link text-dark" href="{{path('aproposdenous')}}">À propos de nous</a>
				</li>
				<li class="nav-item4">
					<a class="nav-link text-dark" href="{{path('contact')}}">Contactez nous</a>
				</li>
					</li><li class="nav-item5">
						<a class="nav-link text-dark" href="{{path('faq')}}">FAQ</a>
					</li>

			</li>
		</ul>
		{#
											<form class="d-flex">
											<input class="form-control" type="search" placeholder="Rechercher..." aria-label="Search">
											<button class="btn btn-outline-success" type="submit">
											<i class="fa fa-search"></i>
											</button>
											</form>#}
	{% endif %}
</div></div></nav>{% if is_granted('ROLE_ADMIN') %}<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #fd6c9e ;">
<div class="container-fluid">
	<a class="navbar-brand text-darktext-danger" href="#">⚠️ Menu Administrateur ⚠️</a>
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
 <a class="nav-link" href="{{path('users_index')}}">⚙️ Utilisateur</a>
	<a class="nav-link" href="{{path('produit_boutique_index')}}">⚙️Produit</a>
	    <a class="nav-link" href="{{path('app_commande_index')}}">⚙️ Commande</a>
	</div>
</div></nav>{% endif %}</header>
