{% extends "base.html.twig" %}

{% block title %}Modification du profil de {{ app.user.prenom}} {{ app.user.nom}}{% endblock %}

{% block body %}
	<main>
		<div class="container mt-5 mt-lg-5">
			<div class="mt-3 pt-2">
				<h3 class="text-center">📝 Modification du profil de {{ app.user.prenom}} {{ app.user.nom}} 📝 !</h3>
			</div>
		</div>
        <div class="container mt-5 mt-lg-5">
			<div class="row justify-content-center">
				{{ form(form) }}
			</div>
		</div>
	</main>
{% endblock %}
1