{% extends 'base.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block body %}
    <h1>Adresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.id }}</td>
            </tr>
            <tr>
                <th>Votre nom au complet</th>
                <td>{{ adresse.fullname }}</td>
            </tr>
            <tr>
                <th>Adresse1</th>
                <td>{{ adresse.adresse1 }}</td>
            </tr>
            <tr>
                <th>Adresse2</th>
                <td>{{ adresse.adresse2 }}</td>
            </tr>
            <tr>
                <th>Complement</th>
                <td>{{ adresse.complement }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adresse.ville }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ adresse.codePostal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adresse.pays }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ adresse.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moncompte') }}"> Revenir en arrière </a>

    <a href="{{ path('app_adresse_edit', {'id': adresse.id}) }}">Modifier</a>

    {{ include('adresse/_delete_form.html.twig') }}
{% endblock %}
