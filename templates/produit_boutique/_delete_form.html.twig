<form method="post" action="{{ path('produit_boutique_delete', {'id': produit_boutique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_boutique.id) }}">
    <button class="btn">Supprimer</button>
</form>
